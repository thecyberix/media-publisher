name: Catch up cross-type title Videos

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Analyze only; do not create Airtable rows
        type: boolean
        default: false
      all_cross_type:
        description: Include all pkgTn Videos with cross-type titles (not only today's log list)
        type: boolean
        default: false

concurrency:
  group: catalog-workflow
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  catchup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-env
        with:
          install-playwright: "true"
          install-ffmpeg: "true"
          extras: thumbnails

      - name: Restore translation corpus
        run: |
          mkdir -p data/corpus
          tar -xzf data/translation-corpus.tar.gz -C data/corpus

      - name: Restore Smartcat session
        env:
          SMARTCAT_STORAGE_STATE_JSON: ${{ secrets.SMARTCAT_STORAGE_STATE_JSON }}
        run: |
          if [ -z "$SMARTCAT_STORAGE_STATE_JSON" ]; then
            echo "No SMARTCAT_STORAGE_STATE_JSON secret configured"
            exit 1
          fi
          printf '%s' "$SMARTCAT_STORAGE_STATE_JSON" > smartcat-state.json

      - name: Restore Canva token
        env:
          CANVA_TOKEN_JSON: ${{ secrets.CANVA_TOKEN_JSON }}
        run: |
          if [ -n "$CANVA_TOKEN_JSON" ]; then
            mkdir -p credentials
            printf '%s' "$CANVA_TOKEN_JSON" > credentials/canva-token.json
          fi

      - name: Catch up cross-type title Videos
        env:
          AIRTABLE_TOKEN: ${{ secrets.AIRTABLE_TOKEN }}
          AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
          AIRTABLE_TABLE_NAME: ${{ secrets.AIRTABLE_TABLE_NAME }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          SMARTCAT_STORAGE_STATE: smartcat-state.json
          SMARTCAT_AI_PREFILL: ${{ vars.SMARTCAT_AI_PREFILL || secrets.SMARTCAT_AI_PREFILL || 'true' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TRANSLATION_PROVIDER: ${{ vars.TRANSLATION_PROVIDER || 'anthropic' }}
          CANVA_CLIENT_ID: ${{ secrets.CANVA_CLIENT_ID }}
          CANVA_CLIENT_SECRET: ${{ secrets.CANVA_CLIENT_SECRET }}
          CANVA_TOKEN_SYNC_PAT: ${{ secrets.CANVA_TOKEN_SYNC_PAT }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PYTHONUNBUFFERED: "1"
        run: |
          flags=()
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            flags+=(--dry-run)
          fi
          if [ "${{ inputs.all_cross_type }}" = "true" ]; then
            flags+=(--all-cross-type)
          fi
          python -u scripts/_ingest_cross_type_title_videos.py "${flags[@]}"
